<nav class="sticky top-0 z-50 w-full bg-[#0a0a0c]/90 backdrop-blur-xl border-b border-zinc-800/80">
  <div class="w-full px-4 sm:px-8 lg:px-12 xl:px-16">
    <div class="flex items-center justify-between h-18 lg:h-20 gap-4 lg:gap-8">
      
      <!-- Brand Logo & Main Nav -->
      <div class="flex items-center gap-8 lg:gap-12">
        <!-- Logo -->
        <%= link_to root_path, class: "flex items-center gap-3 group shrink-0" do %>
          <div class="w-10 h-10 rounded-2xl bg-gradient-to-br from-purple-600/30 to-purple-900/50 border border-purple-500/30 flex items-center justify-center text-purple-400 group-hover:border-purple-400/60 group-hover:shadow-[0_0_20px_rgba(168,85,247,0.35)] transition duration-200">
            <svg class="w-5 h-5 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z" />
            </svg>
          </div>
          <span class="text-2xl font-black tracking-tight text-white">
            Cine<span class="text-purple-400">Log</span>
          </span>
        <% end %>

        <!-- Navigation Links (Desktop) -->
        <%
          req_p = request.path
          is_h = (req_p == root_path || req_p == "/movies")
          is_g = req_p.start_with?("/katalog")
          is_w = req_p.start_with?("/watchlists")
          is_c = req_p.start_with?("/community")
        %>
        <div class="hidden md:flex items-center gap-1.5 lg:gap-2">
          <%= link_to root_path, class: "px-4 py-2 rounded-xl text-sm font-semibold transition-all duration-150 flex items-center gap-2 #{is_h ? 'bg-purple-600/15 text-purple-300 border border-purple-500/25' : 'text-zinc-300 hover:text-white hover:bg-zinc-800/60'}" do %>
            <svg class="w-4 h-4 #{is_h ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            <span>Ana Sayfa</span>
          <% end %>

          <!-- Katalog Dropdown (Türler) -->
          <div class="relative group">
            <%= link_to genres_path, class: "px-4 py-2 rounded-xl text-sm font-semibold transition-all duration-150 flex items-center gap-1.5 #{is_g ? 'bg-purple-600/15 text-purple-300 border border-purple-500/25' : 'text-zinc-300 hover:text-white hover:bg-zinc-800/60'}" do %>
              <svg class="w-4 h-4 #{is_g ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
              </svg>
              <span>Katalog</span>
              <svg class="w-3.5 h-3.5 text-zinc-400 group-hover:text-white transition-transform duration-150 group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            <% end %>

            <!-- Dropdown Menu (Continuous hover container without gap) -->
            <div class="absolute left-0 top-full pt-1.5 w-56 opacity-0 pointer-events-none group-hover:opacity-100 group-hover:pointer-events-auto transition-all duration-150 z-50">
              <div class="bg-[#121217]/95 backdrop-blur-2xl border border-purple-500/20 rounded-2xl shadow-[0_20px_50px_rgba(0,0,0,0.8)] py-2 divide-y divide-zinc-800/60">
                <div class="px-4 py-1.5 text-[11px] font-bold uppercase tracking-wider text-zinc-400 mb-0.5">
                  Katalog Türleri
                </div>
                <div class="py-1">
                  <%= link_to genre_path(878), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Bilimkurgu</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                  <%= link_to genre_path(28), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Aksiyon</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                  <%= link_to genre_path(18), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Dram</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                  <%= link_to genre_path(35), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Komedi</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                  <%= link_to genre_path(27), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Korku</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                  <%= link_to genre_path(53), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Gerilim</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                  <%= link_to genre_path(16), class: "flex items-center justify-between px-4 py-2 text-xs font-semibold text-zinc-200 hover:bg-zinc-800/70 hover:text-purple-300 transition" do %>
                    <span>Animasyon</span>
                    <span class="text-zinc-600 group-hover:text-purple-400">›</span>
                  <% end %>
                </div>
                <div class="pt-1.5 px-1">
                  <%= link_to genres_path, class: "block px-3 py-1.5 rounded-xl text-xs font-bold text-center text-purple-400 hover:text-white hover:bg-purple-600/20 transition" do %>
                    Tüm Katalog →
                  <% end %>
                </div>
              </div>
            </div>
          </div>

          <%= link_to watchlists_path, class: "px-4 py-2 rounded-xl text-sm font-semibold transition-all duration-150 flex items-center gap-2 #{is_w ? 'bg-purple-600/15 text-purple-300 border border-purple-500/25' : 'text-zinc-300 hover:text-white hover:bg-zinc-800/60'}" do %>
            <svg class="w-4 h-4 #{is_w ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
            </svg>
            <span>İzleme Listem</span>
            <% if user_signed_in? && current_user.watchlists.any? %>
              <span class="ml-1 px-2 py-0.5 text-[11px] font-bold rounded-full bg-purple-600/20 text-purple-300 border border-purple-500/30">
                <%= current_user.watchlists.count %>
              </span>
            <% end %>
          <% end %>

          <%= link_to community_posts_path, class: "px-4 py-2 rounded-xl text-sm font-semibold transition-all duration-150 flex items-center gap-2 #{is_c ? 'bg-purple-600/15 text-purple-300 border border-purple-500/25' : 'text-zinc-300 hover:text-white hover:bg-zinc-800/60'}" do %>
            <svg class="w-4 h-4 #{is_c ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z" />
            </svg>
            <span>Topluluk</span>
          <% end %>
        </div>
      </div>

      <!-- Search & User Profile Actions -->
      <div class="flex items-center gap-3 lg:gap-5">
        <!-- Mobile Search Toggle Button -->
        <button type="button" onclick="document.getElementById('mobile-search-bar').classList.toggle('hidden'); document.getElementById('mobile-search-input')?.focus();" class="sm:hidden p-2 text-zinc-400 hover:text-white hover:bg-zinc-800 rounded-xl transition cursor-pointer" title="Ara">
          <svg class="h-5 w-5 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </button>

        <!-- Search Input Bar with Real-time Autocomplete Dropdown (Desktop & Tablet) -->
        <div class="relative hidden sm:block w-64 md:w-72 lg:w-84 xl:w-96" data-controller="autocomplete" data-autocomplete-url-value="<%= search_suggestions_path %>">
          <%= form_with url: search_path, method: :get, data: { turbo_frame: "_top" } do |f| %>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-3.5 flex items-center pointer-events-none">
                <svg class="h-4 w-4 text-zinc-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
              </div>
              <%= f.text_field :query, value: params[:query], autocomplete: "off", placeholder: "Film, dizi veya tür ara...", data: { autocomplete_target: "input", action: "input->autocomplete#search focus->autocomplete#search" }, class: "w-full pl-10 pr-4 py-2 text-xs sm:text-sm bg-zinc-900/90 border border-zinc-800/90 rounded-2xl text-white placeholder-zinc-500 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 transition-all duration-200 shadow-inner" %>
            </div>
          <% end %>

          <!-- Dropdown Results Container Under the Search Box -->
          <div data-autocomplete-target="results" class="hidden absolute right-0 top-full mt-3 w-80 sm:w-[480px] z-50 bg-[#121217]/95 backdrop-blur-2xl border border-purple-500/20 rounded-2xl shadow-[0_20px_50px_rgba(0,0,0,0.8)] overflow-hidden divide-y divide-zinc-800 max-h-[75vh] overflow-y-auto">
          </div>
        </div>

        <!-- User Authentication -->
        <% if user_signed_in? %>
          <div class="flex items-center gap-2 lg:gap-3">
            <div class="flex items-center gap-2.5 px-3 py-1.5 rounded-2xl bg-zinc-900/80 border border-zinc-800/90 shadow-sm">
              <div class="w-7 h-7 rounded-xl bg-gradient-to-br from-purple-700 to-purple-900 border border-purple-500/30 flex items-center justify-center text-xs font-black text-white shadow">
                <%= current_user.username[0].upcase %>
              </div>
              <span class="text-xs font-semibold text-zinc-200 hidden sm:inline max-w-[120px] truncate"><%= current_user.username %></span>
            </div>

            <%= button_to destroy_user_session_path, method: :delete, class: "p-2 text-zinc-400 hover:text-white hover:bg-zinc-800/80 rounded-xl transition text-xs cursor-pointer border border-transparent hover:border-zinc-700", title: "Çıkış Yap" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
              </svg>
            <% end %>
          </div>
        <% else %>
          <div class="flex items-center gap-2 sm:gap-3">
            <%= link_to "Giriş Yap", new_user_session_path, class: "px-4 py-2 text-xs sm:text-sm font-semibold text-zinc-300 hover:text-white transition" %>
            <%= link_to "Kayıt Ol", new_user_registration_path, class: "px-4 py-2 text-xs sm:text-sm font-semibold text-white bg-purple-600 hover:bg-purple-500 border border-purple-500/40 rounded-xl shadow-lg shadow-purple-600/25 transition duration-150" %>
          </div>
        <% end %>
      </div>

    </div>
  </div>

  <!-- Mobile Collapsible Search Bar (with Autocomplete) -->
  <div id="mobile-search-bar" class="hidden sm:hidden border-t border-zinc-800/80 bg-[#121217] px-4 py-3" data-controller="autocomplete" data-autocomplete-url-value="<%= search_suggestions_path %>">
    <%= form_with url: search_path, method: :get, data: { turbo_frame: "_top" } do |f| %>
      <div class="relative flex items-center">
        <div class="absolute inset-y-0 left-0 pl-3.5 flex items-center pointer-events-none">
          <svg class="h-4 w-4 text-zinc-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </div>
        <%= f.text_field :query, id: "mobile-search-input", value: params[:query], autocomplete: "off", placeholder: "Film, dizi veya tür ara...", data: { autocomplete_target: "input", action: "input->autocomplete#search focus->autocomplete#search" }, class: "w-full pl-10 pr-16 py-2.5 text-sm bg-zinc-900 border border-zinc-800 rounded-xl text-white placeholder-zinc-500 focus:outline-none focus:border-purple-500 focus:ring-1 focus:ring-purple-500 transition" %>
        <button type="submit" class="absolute right-1.5 px-3 py-1.5 text-xs font-bold rounded-lg bg-purple-600 text-white cursor-pointer shadow">Ara</button>
      </div>
    <% end %>
    <div data-autocomplete-target="results" class="hidden mt-3 w-full z-50 bg-[#121217]/95 backdrop-blur-2xl border border-purple-500/20 rounded-2xl shadow-[0_20px_50px_rgba(0,0,0,0.8)] overflow-hidden divide-y divide-zinc-800 max-h-[60vh] overflow-y-auto">
    </div>
  </div>
</nav>

<!-- Modern Fixed Bottom Navigation Bar (Mobile Only, App-like feel) -->
<div class="md:hidden fixed bottom-0 inset-x-0 z-40 bg-[#0a0a0c]/95 backdrop-blur-xl border-t border-zinc-800/80 px-2 py-2 shadow-[0_-4px_25px_rgba(0,0,0,0.7)]">
  <%
    current_p = request.path
    is_home = (current_p == root_path || current_p == "/movies")
    is_genres = current_p.start_with?("/katalog")
    is_search = current_p.start_with?("/search")
    is_watchlist = current_p.start_with?("/watchlists")
    is_community = current_p.start_with?("/community")
  %>
  <div class="flex items-center justify-around">
    <!-- Ana Sayfa -->
    <%= link_to root_path, class: "flex flex-col items-center py-1 px-3 rounded-xl transition duration-150 #{is_home ? 'text-purple-400' : 'text-zinc-400 hover:text-white'}" do %>
      <div class="relative">
        <svg class="w-5 h-5 #{is_home ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
        </svg>
        <% if is_home %>
          <span class="absolute -bottom-1.5 left-1/2 -translate-x-1/2 w-1.5 h-1.5 rounded-full bg-purple-400"></span>
        <% end %>
      </div>
      <span class="text-[10px] font-semibold mt-1">Ana Sayfa</span>
    <% end %>

    <!-- Katalog -->
    <%= link_to genres_path, class: "flex flex-col items-center py-1 px-3 rounded-xl transition duration-150 #{is_genres ? 'text-purple-400' : 'text-zinc-400 hover:text-white'}" do %>
      <div class="relative">
        <svg class="w-5 h-5 #{is_genres ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
        </svg>
        <% if is_genres %>
          <span class="absolute -bottom-1.5 left-1/2 -translate-x-1/2 w-1.5 h-1.5 rounded-full bg-purple-400"></span>
        <% end %>
      </div>
      <span class="text-[10px] font-semibold mt-1">Katalog</span>
    <% end %>

    <!-- Ara -->
    <%= link_to search_path, class: "flex flex-col items-center py-1 px-3 rounded-xl transition duration-150 #{is_search ? 'text-purple-400' : 'text-zinc-400 hover:text-white'}" do %>
      <div class="relative">
        <svg class="w-5 h-5 #{is_search ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        <% if is_search %>
          <span class="absolute -bottom-1.5 left-1/2 -translate-x-1/2 w-1.5 h-1.5 rounded-full bg-purple-400"></span>
        <% end %>
      </div>
      <span class="text-[10px] font-semibold mt-1">Ara</span>
    <% end %>

    <!-- İzleme Listem -->
    <%= link_to watchlists_path, class: "flex flex-col items-center py-1 px-3 rounded-xl transition duration-150 #{is_watchlist ? 'text-purple-400' : 'text-zinc-400 hover:text-white'}" do %>
      <div class="relative">
        <svg class="w-5 h-5 #{is_watchlist ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
        </svg>
        <% if user_signed_in? && current_user.watchlists.any? %>
          <span class="absolute -top-1 -right-2 px-1.5 py-0.2 text-[9px] font-black rounded-full bg-purple-600 text-white shadow">
            <%= current_user.watchlists.count %>
          </span>
        <% end %>
        <% if is_watchlist %>
          <span class="absolute -bottom-1.5 left-1/2 -translate-x-1/2 w-1.5 h-1.5 rounded-full bg-purple-400"></span>
        <% end %>
      </div>
      <span class="text-[10px] font-semibold mt-1">Listem</span>
    <% end %>

    <!-- Topluluk -->
    <%= link_to community_posts_path, class: "flex flex-col items-center py-1 px-3 rounded-xl transition duration-150 #{is_community ? 'text-purple-400' : 'text-zinc-400 hover:text-white'}" do %>
      <div class="relative">
        <svg class="w-5 h-5 #{is_community ? 'text-purple-400' : 'text-zinc-400'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z" />
        </svg>
        <% if is_community %>
          <span class="absolute -bottom-1.5 left-1/2 -translate-x-1/2 w-1.5 h-1.5 rounded-full bg-purple-400"></span>
        <% end %>
      </div>
      <span class="text-[10px] font-semibold mt-1">Topluluk</span>
    <% end %>
  </div>
</div>
